<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:padding="@dimen/row_message_padding">

    <ImageView
        android:id="@+id/icon"
        android:layout_width="@dimen/row_message_image_width"
        android:layout_height="@dimen/row_message_image_height"
        tools:src="@drawable/ic_menu_send"/>

    <TextView
        android:id="@+id/name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/icon"
        android:layout_toEndOf="@id/icon"
        android:layout_alignTop="@id/icon"
        android:layout_margin="@dimen/row_message_name_margin"
        android:textStyle="bold"
        tools:text="Kevin Yeh"/>

    <TextView
        android:id="@+id/timestamp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/name"
        android:layout_toEndOf="@id/name"
        android:layout_alignTop="@id/name"
        tools:text="5:15 PM"/>

    <TextView
        android:id="@+id/text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/icon"
        android:layout_toEndOf="@id/icon"
        android:layout_below="@id/name"
        android:layout_margin="@dimen/row_message_name_margin"
        tools:text="@string/hipster_ipsum"/>
</>